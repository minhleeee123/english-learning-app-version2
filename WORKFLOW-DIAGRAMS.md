# 🎨 VISUAL WORKFLOW DIAGRAMS

## 📊 APP ARCHITECTURE OVERVIEW

```
┌─────────────────────────────────────────────────────────────┐
│                        BROWSER                              │
│  ┌───────────────────────────────────────────────────────┐ │
│  │              index.html (Entry Point)                  │ │
│  │  - Load Tailwind CSS                                   │ │
│  │  - Load React from CDN                                 │ │
│  │  - Mount <div id="root">                               │ │
│  └────────────────────┬────────────────────────────────────┘ │
│                       │                                      │
│                       ▼                                      │
│  ┌───────────────────────────────────────────────────────┐ │
│  │            src/index.tsx (React Root)                  │ │
│  │  ReactDOM.render(<App />)                              │ │
│  └────────────────────┬────────────────────────────────────┘ │
│                       │                                      │
│                       ▼                                      │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                 src/App.tsx                            │ │
│  │  ┌─────────────────────────────────────────────────┐  │ │
│  │  │ STATE:                                           │  │ │
│  │  │ - mode: AppMode                                  │  │ │
│  │  │ - decks: Deck[]                                  │  │ │
│  │  │ - selectedDeckId: string                         │  │ │
│  │  │ - loading: boolean                               │  │ │
│  │  └─────────────────────────────────────────────────┘  │ │
│  │                                                         │ │
│  │  ┌──────────────┬──────────────┬──────────────────┐   │ │
│  │  ▼              ▼              ▼                  ▼   │ │
│  │  Dashboard   DeckBuilder   FlashcardMode   QuizMode   │ │
│  │                                              ReadingMode│ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
           │                           │
           │ API Calls                 │ API Calls
           ▼                           ▼
    ┌──────────────┐          ┌──────────────┐
    │   Supabase   │          │  Gemini AI   │
    │  (Database)  │          │   (OpenAI)   │
    └──────────────┘          └──────────────┘
```

---

## 🔄 DATA FLOW

### **1. Initial Load Flow**

```
User opens browser
       │
       ▼
┌──────────────────┐
│  index.html      │ Load HTML + CSS
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ src/index.tsx    │ Initialize React
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  src/App.tsx     │ Mount component
└────────┬─────────┘
         │
         │ useEffect(() => loadDecks())
         │
         ▼
┌──────────────────┐
│services/         │
│supabase.ts       │ fetchAllDecks()
└────────┬─────────┘
         │
         │ SQL Query
         ▼
┌──────────────────┐
│  Supabase DB     │
│  ┌────────────┐  │
│  │ decks      │  │ SELECT *
│  └────────────┘  │
│  ┌────────────┐  │
│  │ words      │  │ SELECT *
│  └────────────┘  │
└────────┬─────────┘
         │
         │ Return JSON
         ▼
┌──────────────────┐
│  App.tsx         │
│  setDecks(data)  │ Update state
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Dashboard       │ Render UI
│  Show decks      │
└──────────────────┘
```

---

## 🆕 CREATE DECK FLOW

```
User clicks "Create New Deck"
       │
       ▼
┌──────────────────┐
│  App.tsx         │
│  setMode(        │
│    DECK_BUILDER) │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────┐
│  DeckBuilder.tsx                     │
│  ┌────────────────────────────────┐  │
│  │ User Input:                    │  │
│  │ - deckName: "Business Words"   │  │
│  │ - rawWords: "negotiate, ..."   │  │
│  └────────────────────────────────┘  │
└────────┬─────────────────────────────┘
         │
         │ User clicks "Create Deck"
         ▼
┌──────────────────┐
│services/         │
│gemini.ts         │ processVocabularyList()
└────────┬─────────┘
         │
         │ API Request
         ▼
┌──────────────────────────────────────┐
│  Gemini AI                           │
│  Prompt: "Process these words..."    │
│                                      │
│  Response:                           │
│  [                                   │
│    {                                 │
│      word: "negotiate",              │
│      ipa: "/nɪˈɡoʊ.ʃi.eɪt/",         │
│      pos: "verb",                    │
│      definition: "Đàm phán...",      │
│      example: "We need to..."        │
│    },                                │
│    ...                               │
│  ]                                   │
└────────┬─────────────────────────────┘
         │
         │ Return processed words
         ▼
┌──────────────────┐
│  DeckBuilder     │
│  Create deck obj │
└────────┬─────────┘
         │
         │ onDeckCreated(newDeck)
         ▼
┌──────────────────┐
│  App.tsx         │
│  createDeck()    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│services/         │
│supabase.ts       │
└────────┬─────────┘
         │
         │ INSERT INTO decks
         │ INSERT INTO words
         ▼
┌──────────────────┐
│  Supabase DB     │
│  ✅ Data saved   │
└────────┬─────────┘
         │
         │ Success response
         ▼
┌──────────────────┐
│  App.tsx         │
│  loadDecks()     │ Reload data
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Dashboard       │
│  Show new deck   │
└──────────────────┘
```

---

## 📚 FLASHCARD MODE FLOW

```
User clicks "Study Flashcards" on a deck
       │
       ▼
┌──────────────────┐
│  App.tsx         │
│  startSession(   │
│    deckId,       │
│    FLASHCARDS)   │
└────────┬─────────┘
         │
         │ setMode(FLASHCARDS)
         │ setSelectedDeckId(deckId)
         ▼
┌──────────────────────────────────────┐
│  FlashcardMode.tsx                   │
│  ┌────────────────────────────────┐  │
│  │ STATE:                         │  │
│  │ - currentIndex: 0              │  │
│  │ - isFlipped: false             │  │
│  │ - words: [...deck.words]       │  │
│  └────────────────────────────────┘  │
└────────┬─────────────────────────────┘
         │
         │ Render current word
         ▼
┌──────────────────────────────────────┐
│  Flashcard UI                        │
│  ┌────────────────────────────────┐  │
│  │  FRONT (isFlipped = false)     │  │
│  │  ┌──────────────────────────┐  │  │
│  │  │ Word: "negotiate"        │  │  │
│  │  │ IPA: /nɪˈɡoʊ.ʃi.eɪt/     │  │  │
│  │  │ Example: "We need to..." │  │  │
│  │  └──────────────────────────┘  │  │
│  │                                │  │
│  │  BACK (isFlipped = true)       │  │
│  │  ┌──────────────────────────┐  │  │
│  │  │ Definition:              │  │  │
│  │  │ "Đàm phán, thương lượng" │  │  │
│  │  └──────────────────────────┘  │  │
│  └────────────────────────────────┘  │
│                                      │
│  [Previous] [Flip] [Next]            │
└──────────────────────────────────────┘
         │
         │ User interactions:
         │
         ├─ Click Flip ──────────────┐
         │                            │
         │                            ▼
         │                   setIsFlipped(!isFlipped)
         │                   CSS: rotateY(180deg)
         │
         ├─ Click Next ──────────────┐
         │                            │
         │                            ▼
         │                   setCurrentIndex(index + 1)
         │                   setIsFlipped(false)
         │
         └─ Click Previous ──────────┐
                                      │
                                      ▼
                             setCurrentIndex(index - 1)
                             setIsFlipped(false)
```

---

## 🧩 QUIZ MODE FLOW

```
User clicks "Quiz" on a deck
       │
       ▼
┌──────────────────┐
│  App.tsx         │
│  startSession(   │
│    deckId,       │
│    QUIZ)         │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────┐
│  QuizMode.tsx                        │
│  ┌────────────────────────────────┐  │
│  │ On Mount:                      │  │
│  │ questions = generateQuiz()     │  │
│  └────────────────────────────────┘  │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│  generateQuiz() Logic                │
│                                      │
│  For each word in deck:              │
│  ┌────────────────────────────────┐  │
│  │ 1. targetWord = current word   │  │
│  │ 2. Get 3 random other words    │  │
│  │ 3. Create 4 options:           │  │
│  │    - 1 correct (target def)    │  │
│  │    - 3 wrong (other defs)      │  │
│  │ 4. Shuffle options             │  │
│  │ 5. Return QuizQuestion object  │  │
│  └────────────────────────────────┘  │
└────────┬─────────────────────────────┘
         │
         │ questions: QuizQuestion[]
         ▼
┌──────────────────────────────────────┐
│  Quiz UI                             │
│  ┌────────────────────────────────┐  │
│  │ Question 1/10                  │  │
│  │                                │  │
│  │ Word: negotiate                │  │
│  │ IPA: /nɪˈɡoʊ.ʃi.eɪt/           │  │
│  │ Example: "We need to..."       │  │
│  │                                │  │
│  │ Which definition is correct?   │  │
│  │                                │  │
│  │ ○ A. Đàm phán (CORRECT)        │  │
│  │ ○ B. Phân tích                 │  │
│  │ ○ C. Thiết lập                 │  │
│  │ ○ D. Xác định                  │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
         │
         │ User clicks option
         ▼
┌──────────────────┐
│  handleAnswer()  │
│  ┌────────────┐  │
│  │ Check if   │  │
│  │ correct    │  │
│  └────────────┘  │
└────────┬─────────┘
         │
         ├─ If correct ────────┐
         │                     │
         │                     ▼
         │            setScore(score + 1)
         │            Show green ✅
         │
         └─ If wrong ─────────┐
                              │
                              ▼
                     Show red ❌
                     Show correct answer
         
         Both paths:
         │
         │ Wait 1.5 seconds
         ▼
    Move to next question
         │
         ▼
    Repeat until all questions done
         │
         ▼
┌──────────────────────────────────────┐
│  Final Score Screen                  │
│  ┌────────────────────────────────┐  │
│  │ 🎉 Quiz Complete!              │  │
│  │                                │  │
│  │ Your Score: 8/10               │  │
│  │ Accuracy: 80%                  │  │
│  │                                │  │
│  │ [Retry] [Exit]                 │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

---

## 📖 READING MODE FLOW

```
User clicks "Reading" on a deck
       │
       ▼
┌──────────────────┐
│  App.tsx         │
│  startSession(   │
│    deckId,       │
│    READING)      │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────┐
│  ReadingMode.tsx                     │
│  ┌────────────────────────────────┐  │
│  │ useEffect(() => {              │  │
│  │   fetchReading()               │  │
│  │ })                             │  │
│  └────────────────────────────────┘  │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────┐
│services/         │
│gemini.ts         │ generateReadingPassage()
└────────┬─────────┘
         │
         │ API Request
         ▼
┌──────────────────────────────────────┐
│  Gemini AI                           │
│  Prompt:                             │
│  "Write a short story using these    │
│   words: negotiate, entrepreneur..." │
│                                      │
│  Response:                           │
│  {                                   │
│    title: "A Business Meeting",     │
│    content: "John wanted to         │
│      {{negotiate}} a deal with...   │
│      As an {{entrepreneur}}, he..." │
│    questions: [                     │
│      {                              │
│        question: "What did John     │
│          want to do?",              │
│        options: [...],              │
│        correctAnswerIndex: 0        │
│      }                              │
│    ]                                │
│  }                                  │
└────────┬─────────────────────────────┘
         │
         │ Return reading data
         ▼
┌──────────────────────────────────────┐
│  ReadingMode.tsx                     │
│  setReadingData(data)                │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│  Reading UI                          │
│  ┌────────────────────────────────┐  │
│  │ 📖 A Business Meeting          │  │
│  │                                │  │
│  │ John wanted to negotiate a     │  │
│  │        ^^^^^^^^^ (highlighted) │  │
│  │ deal with a large company. As  │  │
│  │ an entrepreneur, he understood │  │
│  │    ^^^^^^^^^^^^ (highlighted)  │  │
│  │ the importance of...           │  │
│  │                                │  │
│  │ ────────────────────────────── │  │
│  │                                │  │
│  │ 📝 Comprehension Questions:    │  │
│  │                                │  │
│  │ 1. What did John want to do?   │  │
│  │    ○ A. Negotiate a deal ✓     │  │
│  │    ○ B. Start a company        │  │
│  │    ○ C. Hire employees         │  │
│  │    ○ D. Close a business       │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
         │
         │ User reads and answers
         ▼
    Show score & feedback
```

---

## 🗑️ DELETE DECK FLOW

```
User hovers over deck card
       │
       ▼
┌──────────────────┐
│  Dashboard       │
│  Show delete btn │
└────────┬─────────┘
         │
         │ User clicks delete (trash icon)
         ▼
┌──────────────────┐
│  App.tsx         │
│  handleDeleteDeck│
└────────┬─────────┘
         │
         │ Confirm dialog
         ▼
    window.confirm("Are you sure?")
         │
         ├─ Cancel ────────┐
         │                 │
         │                 ▼
         │             Do nothing
         │
         └─ OK ────────────┐
                           │
                           ▼
                   ┌──────────────────┐
                   │services/         │
                   │supabase.ts       │
                   │  deleteDeck()    │
                   └────────┬─────────┘
                            │
                            │ DELETE FROM decks
                            │ WHERE id = deckId
                            ▼
                   ┌──────────────────┐
                   │  Supabase DB     │
                   │  ┌────────────┐  │
                   │  │ decks      │  │
                   │  │ ❌ Deleted │  │
                   │  └────────────┘  │
                   │  ┌────────────┐  │
                   │  │ words      │  │
                   │  │ ❌ CASCADE │  │
                   │  │   DELETE   │  │
                   │  └────────────┘  │
                   └────────┬─────────┘
                            │
                            │ Success
                            ▼
                   ┌──────────────────┐
                   │  App.tsx         │
                   │  loadDecks()     │
                   └────────┬─────────┘
                            │
                            ▼
                   ┌──────────────────┐
                   │  Dashboard       │
                   │  Deck removed    │
                   └──────────────────┘
```

---

## 🔐 AUTHENTICATION FLOW (Future)

```
┌──────────────────────────────────────┐
│  Future Enhancement:                 │
│  Add Supabase Auth                   │
│                                      │
│  User Login                          │
│       │                              │
│       ▼                              │
│  Supabase Auth                       │
│       │                              │
│       ▼                              │
│  Generate JWT Token                  │
│       │                              │
│       ▼                              │
│  Store in localStorage               │
│       │                              │
│       ▼                              │
│  Include in all API requests         │
│       │                              │
│       ▼                              │
│  RLS Policies check user_id          │
│       │                              │
│       ▼                              │
│  Return only user's data             │
│                                      │
│  Benefits:                           │
│  ✅ Private decks                    │
│  ✅ Multi-device sync                │
│  ✅ Progress tracking                │
│  ✅ Social features                  │
└──────────────────────────────────────┘
```

---

## 📊 STATE MANAGEMENT DIAGRAM

```
┌────────────────────────────────────────────────┐
│                App.tsx (Root State)            │
├────────────────────────────────────────────────┤
│                                                │
│  mode: AppMode                                 │
│    ├─ DASHBOARD ──────► Show deck list         │
│    ├─ DECK_BUILDER ───► Create new deck        │
│    ├─ FLASHCARDS ─────► Study mode             │
│    ├─ QUIZ ───────────► Quiz mode              │
│    └─ READING ────────► Reading mode           │
│                                                │
│  decks: Deck[]                                 │
│    └─ Array of all decks from database         │
│                                                │
│  selectedDeckId: string | null                 │
│    └─ Currently active deck for learning       │
│                                                │
│  loading: boolean                              │
│    └─ Show spinner while fetching data         │
│                                                │
└────────────────────────────────────────────────┘
         │ Props passed down to children
         │
    ┌────┴────┬──────────┬─────────┬──────────┐
    │         │          │         │          │
    ▼         ▼          ▼         ▼          ▼
Dashboard DeckBuilder Flashcard Quiz    Reading
         (own state) (own state)(own state)(own state)
```

---

**🎨 Visual diagrams giúp hiểu luồng hoạt động dễ dàng hơn!**
